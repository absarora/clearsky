<%# app/views/forecasts/index.html.erb
  #
  # Landing page. The search form lives in the application layout so it
  # persists across the index and show views without a page reload.
  # This Turbo Frame is the target for forecast results on submission.
%>

<%= turbo_frame_tag "forecast" %>
